# Encoding Rules

- Always use UTF-8 without BOM
- Never use GBK or ANSI
- When reading/writing files, explicitly specify UTF-8
- Never rewrite garbled Chinese text
- Preserve existing Chinese characters
- Use PowerShell 7 (pwsh)